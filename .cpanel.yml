---
deployment:
  tasks:
    - export DEPLOYPATH=/home/a2bremov/davidswinstead.comn/misc/typescript
    - /bin/cp -R * $DEPLOYPATH
    - cd $DEPLOYPATH
    - npm install
    - ./node_modules/.bin/tsc
